/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { STAYKE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const INITIALIZE_CONTRACT_DISCRIMINATOR = new Uint8Array([
  181, 192, 35, 141, 212, 113, 138, 94,
]);

export function getInitializeContractDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_CONTRACT_DISCRIMINATOR,
  );
}

export type InitializeContractInstruction<
  TProgram extends string = typeof STAYKE_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountTreasuryPda extends string | AccountMeta<string> = string,
  TAccountTreasuryTokenAccount extends string | AccountMeta<string> = string,
  TAccountPlatformVaultPda extends string | AccountMeta<string> = string,
  TAccountPlatformVaultTokenAccount extends string | AccountMeta<string> =
    string,
  TAccountUsdcMint extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      TAccountTreasuryPda extends string
        ? ReadonlyAccount<TAccountTreasuryPda>
        : TAccountTreasuryPda,
      TAccountTreasuryTokenAccount extends string
        ? WritableAccount<TAccountTreasuryTokenAccount>
        : TAccountTreasuryTokenAccount,
      TAccountPlatformVaultPda extends string
        ? ReadonlyAccount<TAccountPlatformVaultPda>
        : TAccountPlatformVaultPda,
      TAccountPlatformVaultTokenAccount extends string
        ? WritableAccount<TAccountPlatformVaultTokenAccount>
        : TAccountPlatformVaultTokenAccount,
      TAccountUsdcMint extends string
        ? ReadonlyAccount<TAccountUsdcMint>
        : TAccountUsdcMint,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeContractInstructionData = {
  discriminator: ReadonlyUint8Array;
  admins: Array<Address>;
  retributionBpsLow: number;
  retributionBpsMedium: number;
  retributionBpsHigh: number;
  minimumDeposit: bigint;
  /** Platform fee charged per completed booking, in basis points (e.g. 500 = 5%). */
  feeBps: number;
};

export type InitializeContractInstructionDataArgs = {
  admins: Array<Address>;
  retributionBpsLow: number;
  retributionBpsMedium: number;
  retributionBpsHigh: number;
  minimumDeposit: number | bigint;
  /** Platform fee charged per completed booking, in basis points (e.g. 500 = 5%). */
  feeBps: number;
};

export function getInitializeContractInstructionDataEncoder(): Encoder<InitializeContractInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["admins", getArrayEncoder(getAddressEncoder())],
      ["retributionBpsLow", getU16Encoder()],
      ["retributionBpsMedium", getU16Encoder()],
      ["retributionBpsHigh", getU16Encoder()],
      ["minimumDeposit", getU64Encoder()],
      ["feeBps", getU16Encoder()],
    ]),
    (value) => ({ ...value, discriminator: INITIALIZE_CONTRACT_DISCRIMINATOR }),
  );
}

export function getInitializeContractInstructionDataDecoder(): Decoder<InitializeContractInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["admins", getArrayDecoder(getAddressDecoder())],
    ["retributionBpsLow", getU16Decoder()],
    ["retributionBpsMedium", getU16Decoder()],
    ["retributionBpsHigh", getU16Decoder()],
    ["minimumDeposit", getU64Decoder()],
    ["feeBps", getU16Decoder()],
  ]);
}

export function getInitializeContractInstructionDataCodec(): Codec<
  InitializeContractInstructionDataArgs,
  InitializeContractInstructionData
> {
  return combineCodec(
    getInitializeContractInstructionDataEncoder(),
    getInitializeContractInstructionDataDecoder(),
  );
}

export type InitializeContractAsyncInput<
  TAccountSigner extends string = string,
  TAccountConfig extends string = string,
  TAccountTreasuryPda extends string = string,
  TAccountTreasuryTokenAccount extends string = string,
  TAccountPlatformVaultPda extends string = string,
  TAccountPlatformVaultTokenAccount extends string = string,
  TAccountUsdcMint extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  config?: Address<TAccountConfig>;
  treasuryPda?: Address<TAccountTreasuryPda>;
  /** Token account controlled by treasury_pda. Holds user guarantee deposits. */
  treasuryTokenAccount?: Address<TAccountTreasuryTokenAccount>;
  platformVaultPda?: Address<TAccountPlatformVaultPda>;
  /** Token account controlled by platform_vault_pda. Holds platform fees. */
  platformVaultTokenAccount?: Address<TAccountPlatformVaultTokenAccount>;
  usdcMint: Address<TAccountUsdcMint>;
  tokenProgram?: Address<TAccountTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  admins: InitializeContractInstructionDataArgs["admins"];
  retributionBpsLow: InitializeContractInstructionDataArgs["retributionBpsLow"];
  retributionBpsMedium: InitializeContractInstructionDataArgs["retributionBpsMedium"];
  retributionBpsHigh: InitializeContractInstructionDataArgs["retributionBpsHigh"];
  minimumDeposit: InitializeContractInstructionDataArgs["minimumDeposit"];
  feeBps: InitializeContractInstructionDataArgs["feeBps"];
};

export async function getInitializeContractInstructionAsync<
  TAccountSigner extends string,
  TAccountConfig extends string,
  TAccountTreasuryPda extends string,
  TAccountTreasuryTokenAccount extends string,
  TAccountPlatformVaultPda extends string,
  TAccountPlatformVaultTokenAccount extends string,
  TAccountUsdcMint extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof STAYKE_PROGRAM_ADDRESS,
>(
  input: InitializeContractAsyncInput<
    TAccountSigner,
    TAccountConfig,
    TAccountTreasuryPda,
    TAccountTreasuryTokenAccount,
    TAccountPlatformVaultPda,
    TAccountPlatformVaultTokenAccount,
    TAccountUsdcMint,
    TAccountTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeContractInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountConfig,
    TAccountTreasuryPda,
    TAccountTreasuryTokenAccount,
    TAccountPlatformVaultPda,
    TAccountPlatformVaultTokenAccount,
    TAccountUsdcMint,
    TAccountTokenProgram,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? STAYKE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
    treasuryPda: { value: input.treasuryPda ?? null, isWritable: false },
    treasuryTokenAccount: {
      value: input.treasuryTokenAccount ?? null,
      isWritable: true,
    },
    platformVaultPda: {
      value: input.platformVaultPda ?? null,
      isWritable: false,
    },
    platformVaultTokenAccount: {
      value: input.platformVaultTokenAccount ?? null,
      isWritable: true,
    },
    usdcMint: { value: input.usdcMint ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([99, 111, 110, 102, 105, 103])),
      ],
    });
  }
  if (!accounts.treasuryPda.value) {
    accounts.treasuryPda.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([116, 114, 101, 97, 115, 117, 114, 121]),
        ),
      ],
    });
  }
  if (!accounts.treasuryTokenAccount.value) {
    accounts.treasuryTokenAccount.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            116, 114, 101, 97, 115, 117, 114, 121, 95, 118, 97, 117, 108, 116,
          ]),
        ),
      ],
    });
  }
  if (!accounts.platformVaultPda.value) {
    accounts.platformVaultPda.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            112, 108, 97, 116, 102, 111, 114, 109, 95, 118, 97, 117, 108, 116,
          ]),
        ),
      ],
    });
  }
  if (!accounts.platformVaultTokenAccount.value) {
    accounts.platformVaultTokenAccount.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            112, 108, 97, 116, 102, 111, 114, 109, 95, 118, 97, 117, 108, 116,
            95, 116, 111, 107, 101, 110,
          ]),
        ),
      ],
    });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.treasuryPda),
      getAccountMeta(accounts.treasuryTokenAccount),
      getAccountMeta(accounts.platformVaultPda),
      getAccountMeta(accounts.platformVaultTokenAccount),
      getAccountMeta(accounts.usdcMint),
      getAccountMeta(accounts.tokenProgram),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeContractInstructionDataEncoder().encode(
      args as InitializeContractInstructionDataArgs,
    ),
    programAddress,
  } as InitializeContractInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountConfig,
    TAccountTreasuryPda,
    TAccountTreasuryTokenAccount,
    TAccountPlatformVaultPda,
    TAccountPlatformVaultTokenAccount,
    TAccountUsdcMint,
    TAccountTokenProgram,
    TAccountSystemProgram
  >);
}

export type InitializeContractInput<
  TAccountSigner extends string = string,
  TAccountConfig extends string = string,
  TAccountTreasuryPda extends string = string,
  TAccountTreasuryTokenAccount extends string = string,
  TAccountPlatformVaultPda extends string = string,
  TAccountPlatformVaultTokenAccount extends string = string,
  TAccountUsdcMint extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  config: Address<TAccountConfig>;
  treasuryPda: Address<TAccountTreasuryPda>;
  /** Token account controlled by treasury_pda. Holds user guarantee deposits. */
  treasuryTokenAccount: Address<TAccountTreasuryTokenAccount>;
  platformVaultPda: Address<TAccountPlatformVaultPda>;
  /** Token account controlled by platform_vault_pda. Holds platform fees. */
  platformVaultTokenAccount: Address<TAccountPlatformVaultTokenAccount>;
  usdcMint: Address<TAccountUsdcMint>;
  tokenProgram?: Address<TAccountTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  admins: InitializeContractInstructionDataArgs["admins"];
  retributionBpsLow: InitializeContractInstructionDataArgs["retributionBpsLow"];
  retributionBpsMedium: InitializeContractInstructionDataArgs["retributionBpsMedium"];
  retributionBpsHigh: InitializeContractInstructionDataArgs["retributionBpsHigh"];
  minimumDeposit: InitializeContractInstructionDataArgs["minimumDeposit"];
  feeBps: InitializeContractInstructionDataArgs["feeBps"];
};

export function getInitializeContractInstruction<
  TAccountSigner extends string,
  TAccountConfig extends string,
  TAccountTreasuryPda extends string,
  TAccountTreasuryTokenAccount extends string,
  TAccountPlatformVaultPda extends string,
  TAccountPlatformVaultTokenAccount extends string,
  TAccountUsdcMint extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof STAYKE_PROGRAM_ADDRESS,
>(
  input: InitializeContractInput<
    TAccountSigner,
    TAccountConfig,
    TAccountTreasuryPda,
    TAccountTreasuryTokenAccount,
    TAccountPlatformVaultPda,
    TAccountPlatformVaultTokenAccount,
    TAccountUsdcMint,
    TAccountTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeContractInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountConfig,
  TAccountTreasuryPda,
  TAccountTreasuryTokenAccount,
  TAccountPlatformVaultPda,
  TAccountPlatformVaultTokenAccount,
  TAccountUsdcMint,
  TAccountTokenProgram,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? STAYKE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
    treasuryPda: { value: input.treasuryPda ?? null, isWritable: false },
    treasuryTokenAccount: {
      value: input.treasuryTokenAccount ?? null,
      isWritable: true,
    },
    platformVaultPda: {
      value: input.platformVaultPda ?? null,
      isWritable: false,
    },
    platformVaultTokenAccount: {
      value: input.platformVaultTokenAccount ?? null,
      isWritable: true,
    },
    usdcMint: { value: input.usdcMint ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.treasuryPda),
      getAccountMeta(accounts.treasuryTokenAccount),
      getAccountMeta(accounts.platformVaultPda),
      getAccountMeta(accounts.platformVaultTokenAccount),
      getAccountMeta(accounts.usdcMint),
      getAccountMeta(accounts.tokenProgram),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeContractInstructionDataEncoder().encode(
      args as InitializeContractInstructionDataArgs,
    ),
    programAddress,
  } as InitializeContractInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountConfig,
    TAccountTreasuryPda,
    TAccountTreasuryTokenAccount,
    TAccountPlatformVaultPda,
    TAccountPlatformVaultTokenAccount,
    TAccountUsdcMint,
    TAccountTokenProgram,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeContractInstruction<
  TProgram extends string = typeof STAYKE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    config: TAccountMetas[1];
    treasuryPda: TAccountMetas[2];
    /** Token account controlled by treasury_pda. Holds user guarantee deposits. */
    treasuryTokenAccount: TAccountMetas[3];
    platformVaultPda: TAccountMetas[4];
    /** Token account controlled by platform_vault_pda. Holds platform fees. */
    platformVaultTokenAccount: TAccountMetas[5];
    usdcMint: TAccountMetas[6];
    tokenProgram: TAccountMetas[7];
    systemProgram: TAccountMetas[8];
  };
  data: InitializeContractInstructionData;
};

export function parseInitializeContractInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeContractInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 9) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      config: getNextAccount(),
      treasuryPda: getNextAccount(),
      treasuryTokenAccount: getNextAccount(),
      platformVaultPda: getNextAccount(),
      platformVaultTokenAccount: getNextAccount(),
      usdcMint: getNextAccount(),
      tokenProgram: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeContractInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}

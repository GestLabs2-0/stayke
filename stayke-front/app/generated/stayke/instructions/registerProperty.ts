/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { STAYKE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const REGISTER_PROPERTY_DISCRIMINATOR = new Uint8Array([
  25, 115, 131, 71, 59, 22, 25, 16,
]);

export function getRegisterPropertyDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REGISTER_PROPERTY_DISCRIMINATOR,
  );
}

export type RegisterPropertyInstruction<
  TProgram extends string = typeof STAYKE_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountUserProfile extends string | AccountMeta<string> = string,
  TAccountProperty extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountUserProfile extends string
        ? WritableAccount<TAccountUserProfile>
        : TAccountUserProfile,
      TAccountProperty extends string
        ? WritableAccount<TAccountProperty>
        : TAccountProperty,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RegisterPropertyInstructionData = {
  discriminator: ReadonlyUint8Array;
  pricePerNight: bigint;
};

export type RegisterPropertyInstructionDataArgs = {
  pricePerNight: number | bigint;
};

export function getRegisterPropertyInstructionDataEncoder(): FixedSizeEncoder<RegisterPropertyInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["pricePerNight", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: REGISTER_PROPERTY_DISCRIMINATOR }),
  );
}

export function getRegisterPropertyInstructionDataDecoder(): FixedSizeDecoder<RegisterPropertyInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["pricePerNight", getU64Decoder()],
  ]);
}

export function getRegisterPropertyInstructionDataCodec(): FixedSizeCodec<
  RegisterPropertyInstructionDataArgs,
  RegisterPropertyInstructionData
> {
  return combineCodec(
    getRegisterPropertyInstructionDataEncoder(),
    getRegisterPropertyInstructionDataDecoder(),
  );
}

export type RegisterPropertyInput<
  TAccountSigner extends string = string,
  TAccountUserProfile extends string = string,
  TAccountProperty extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  userProfile: Address<TAccountUserProfile>;
  property: Address<TAccountProperty>;
  systemProgram?: Address<TAccountSystemProgram>;
  pricePerNight: RegisterPropertyInstructionDataArgs["pricePerNight"];
};

export function getRegisterPropertyInstruction<
  TAccountSigner extends string,
  TAccountUserProfile extends string,
  TAccountProperty extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof STAYKE_PROGRAM_ADDRESS,
>(
  input: RegisterPropertyInput<
    TAccountSigner,
    TAccountUserProfile,
    TAccountProperty,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RegisterPropertyInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountUserProfile,
  TAccountProperty,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? STAYKE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    userProfile: { value: input.userProfile ?? null, isWritable: true },
    property: { value: input.property ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.userProfile),
      getAccountMeta(accounts.property),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getRegisterPropertyInstructionDataEncoder().encode(
      args as RegisterPropertyInstructionDataArgs,
    ),
    programAddress,
  } as RegisterPropertyInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountUserProfile,
    TAccountProperty,
    TAccountSystemProgram
  >);
}

export type ParsedRegisterPropertyInstruction<
  TProgram extends string = typeof STAYKE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    userProfile: TAccountMetas[1];
    property: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: RegisterPropertyInstructionData;
};

export function parseRegisterPropertyInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRegisterPropertyInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      userProfile: getNextAccount(),
      property: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRegisterPropertyInstructionDataDecoder().decode(instruction.data),
  };
}

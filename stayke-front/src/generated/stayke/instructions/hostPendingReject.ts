/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { STAYKE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const HOST_PENDING_REJECT_DISCRIMINATOR = new Uint8Array([
  189, 90, 184, 27, 163, 251, 25, 50,
]);

export function getHostPendingRejectDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    HOST_PENDING_REJECT_DISCRIMINATOR,
  );
}

export type HostPendingRejectInstruction<
  TProgram extends string = typeof STAYKE_PROGRAM_ADDRESS,
  TAccountHost extends string | AccountMeta<string> = string,
  TAccountPropertyHost extends string | AccountMeta<string> = string,
  TAccountGuest extends string | AccountMeta<string> = string,
  TAccountBooking extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountBookingDays extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountHost extends string
        ? WritableSignerAccount<TAccountHost> & AccountSignerMeta<TAccountHost>
        : TAccountHost,
      TAccountPropertyHost extends string
        ? WritableAccount<TAccountPropertyHost>
        : TAccountPropertyHost,
      TAccountGuest extends string
        ? WritableAccount<TAccountGuest>
        : TAccountGuest,
      TAccountBooking extends string
        ? WritableAccount<TAccountBooking>
        : TAccountBooking,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountBookingDays extends string
        ? WritableAccount<TAccountBookingDays>
        : TAccountBookingDays,
      ...TRemainingAccounts,
    ]
  >;

export type HostPendingRejectInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type HostPendingRejectInstructionDataArgs = {};

export function getHostPendingRejectInstructionDataEncoder(): FixedSizeEncoder<HostPendingRejectInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: HOST_PENDING_REJECT_DISCRIMINATOR }),
  );
}

export function getHostPendingRejectInstructionDataDecoder(): FixedSizeDecoder<HostPendingRejectInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getHostPendingRejectInstructionDataCodec(): FixedSizeCodec<
  HostPendingRejectInstructionDataArgs,
  HostPendingRejectInstructionData
> {
  return combineCodec(
    getHostPendingRejectInstructionDataEncoder(),
    getHostPendingRejectInstructionDataDecoder(),
  );
}

export type HostPendingRejectAsyncInput<
  TAccountHost extends string = string,
  TAccountPropertyHost extends string = string,
  TAccountGuest extends string = string,
  TAccountBooking extends string = string,
  TAccountConfig extends string = string,
  TAccountBookingDays extends string = string,
> = {
  host: TransactionSigner<TAccountHost>;
  propertyHost: Address<TAccountPropertyHost>;
  /**
   * We don't want to pass lamports to the host because it didn't create the booking
   * The guest has the responsability to send the money from the PDA to its wallet until I find a way to avoit
   * adding many accounts in this instruction
   */
  guest: Address<TAccountGuest>;
  booking: Address<TAccountBooking>;
  config?: Address<TAccountConfig>;
  bookingDays: Address<TAccountBookingDays>;
};

export async function getHostPendingRejectInstructionAsync<
  TAccountHost extends string,
  TAccountPropertyHost extends string,
  TAccountGuest extends string,
  TAccountBooking extends string,
  TAccountConfig extends string,
  TAccountBookingDays extends string,
  TProgramAddress extends Address = typeof STAYKE_PROGRAM_ADDRESS,
>(
  input: HostPendingRejectAsyncInput<
    TAccountHost,
    TAccountPropertyHost,
    TAccountGuest,
    TAccountBooking,
    TAccountConfig,
    TAccountBookingDays
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  HostPendingRejectInstruction<
    TProgramAddress,
    TAccountHost,
    TAccountPropertyHost,
    TAccountGuest,
    TAccountBooking,
    TAccountConfig,
    TAccountBookingDays
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? STAYKE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    host: { value: input.host ?? null, isWritable: true },
    propertyHost: { value: input.propertyHost ?? null, isWritable: true },
    guest: { value: input.guest ?? null, isWritable: true },
    booking: { value: input.booking ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    bookingDays: { value: input.bookingDays ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([99, 111, 110, 102, 105, 103])),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.host),
      getAccountMeta(accounts.propertyHost),
      getAccountMeta(accounts.guest),
      getAccountMeta(accounts.booking),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.bookingDays),
    ],
    data: getHostPendingRejectInstructionDataEncoder().encode({}),
    programAddress,
  } as HostPendingRejectInstruction<
    TProgramAddress,
    TAccountHost,
    TAccountPropertyHost,
    TAccountGuest,
    TAccountBooking,
    TAccountConfig,
    TAccountBookingDays
  >);
}

export type HostPendingRejectInput<
  TAccountHost extends string = string,
  TAccountPropertyHost extends string = string,
  TAccountGuest extends string = string,
  TAccountBooking extends string = string,
  TAccountConfig extends string = string,
  TAccountBookingDays extends string = string,
> = {
  host: TransactionSigner<TAccountHost>;
  propertyHost: Address<TAccountPropertyHost>;
  /**
   * We don't want to pass lamports to the host because it didn't create the booking
   * The guest has the responsability to send the money from the PDA to its wallet until I find a way to avoit
   * adding many accounts in this instruction
   */
  guest: Address<TAccountGuest>;
  booking: Address<TAccountBooking>;
  config: Address<TAccountConfig>;
  bookingDays: Address<TAccountBookingDays>;
};

export function getHostPendingRejectInstruction<
  TAccountHost extends string,
  TAccountPropertyHost extends string,
  TAccountGuest extends string,
  TAccountBooking extends string,
  TAccountConfig extends string,
  TAccountBookingDays extends string,
  TProgramAddress extends Address = typeof STAYKE_PROGRAM_ADDRESS,
>(
  input: HostPendingRejectInput<
    TAccountHost,
    TAccountPropertyHost,
    TAccountGuest,
    TAccountBooking,
    TAccountConfig,
    TAccountBookingDays
  >,
  config?: { programAddress?: TProgramAddress },
): HostPendingRejectInstruction<
  TProgramAddress,
  TAccountHost,
  TAccountPropertyHost,
  TAccountGuest,
  TAccountBooking,
  TAccountConfig,
  TAccountBookingDays
> {
  // Program address.
  const programAddress = config?.programAddress ?? STAYKE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    host: { value: input.host ?? null, isWritable: true },
    propertyHost: { value: input.propertyHost ?? null, isWritable: true },
    guest: { value: input.guest ?? null, isWritable: true },
    booking: { value: input.booking ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    bookingDays: { value: input.bookingDays ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.host),
      getAccountMeta(accounts.propertyHost),
      getAccountMeta(accounts.guest),
      getAccountMeta(accounts.booking),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.bookingDays),
    ],
    data: getHostPendingRejectInstructionDataEncoder().encode({}),
    programAddress,
  } as HostPendingRejectInstruction<
    TProgramAddress,
    TAccountHost,
    TAccountPropertyHost,
    TAccountGuest,
    TAccountBooking,
    TAccountConfig,
    TAccountBookingDays
  >);
}

export type ParsedHostPendingRejectInstruction<
  TProgram extends string = typeof STAYKE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    host: TAccountMetas[0];
    propertyHost: TAccountMetas[1];
    /**
     * We don't want to pass lamports to the host because it didn't create the booking
     * The guest has the responsability to send the money from the PDA to its wallet until I find a way to avoit
     * adding many accounts in this instruction
     */
    guest: TAccountMetas[2];
    booking: TAccountMetas[3];
    config: TAccountMetas[4];
    bookingDays: TAccountMetas[5];
  };
  data: HostPendingRejectInstructionData;
};

export function parseHostPendingRejectInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedHostPendingRejectInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      host: getNextAccount(),
      propertyHost: getNextAccount(),
      guest: getNextAccount(),
      booking: getNextAccount(),
      config: getNextAccount(),
      bookingDays: getNextAccount(),
    },
    data: getHostPendingRejectInstructionDataDecoder().decode(instruction.data),
  };
}

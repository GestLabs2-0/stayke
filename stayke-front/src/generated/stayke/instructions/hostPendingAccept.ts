/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { STAYKE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const HOST_PENDING_ACCEPT_DISCRIMINATOR = new Uint8Array([
  122, 146, 36, 188, 177, 48, 157, 9,
]);

export function getHostPendingAcceptDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    HOST_PENDING_ACCEPT_DISCRIMINATOR,
  );
}

export type HostPendingAcceptInstruction<
  TProgram extends string = typeof STAYKE_PROGRAM_ADDRESS,
  TAccountHost extends string | AccountMeta<string> = string,
  TAccountPropertyHost extends string | AccountMeta<string> = string,
  TAccountBooking extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountHost extends string
        ? WritableSignerAccount<TAccountHost> & AccountSignerMeta<TAccountHost>
        : TAccountHost,
      TAccountPropertyHost extends string
        ? WritableAccount<TAccountPropertyHost>
        : TAccountPropertyHost,
      TAccountBooking extends string
        ? WritableAccount<TAccountBooking>
        : TAccountBooking,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      ...TRemainingAccounts,
    ]
  >;

export type HostPendingAcceptInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type HostPendingAcceptInstructionDataArgs = {};

export function getHostPendingAcceptInstructionDataEncoder(): FixedSizeEncoder<HostPendingAcceptInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: HOST_PENDING_ACCEPT_DISCRIMINATOR }),
  );
}

export function getHostPendingAcceptInstructionDataDecoder(): FixedSizeDecoder<HostPendingAcceptInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getHostPendingAcceptInstructionDataCodec(): FixedSizeCodec<
  HostPendingAcceptInstructionDataArgs,
  HostPendingAcceptInstructionData
> {
  return combineCodec(
    getHostPendingAcceptInstructionDataEncoder(),
    getHostPendingAcceptInstructionDataDecoder(),
  );
}

export type HostPendingAcceptAsyncInput<
  TAccountHost extends string = string,
  TAccountPropertyHost extends string = string,
  TAccountBooking extends string = string,
  TAccountConfig extends string = string,
> = {
  host: TransactionSigner<TAccountHost>;
  propertyHost: Address<TAccountPropertyHost>;
  booking: Address<TAccountBooking>;
  config?: Address<TAccountConfig>;
};

export async function getHostPendingAcceptInstructionAsync<
  TAccountHost extends string,
  TAccountPropertyHost extends string,
  TAccountBooking extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof STAYKE_PROGRAM_ADDRESS,
>(
  input: HostPendingAcceptAsyncInput<
    TAccountHost,
    TAccountPropertyHost,
    TAccountBooking,
    TAccountConfig
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  HostPendingAcceptInstruction<
    TProgramAddress,
    TAccountHost,
    TAccountPropertyHost,
    TAccountBooking,
    TAccountConfig
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? STAYKE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    host: { value: input.host ?? null, isWritable: true },
    propertyHost: { value: input.propertyHost ?? null, isWritable: true },
    booking: { value: input.booking ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([99, 111, 110, 102, 105, 103])),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.host),
      getAccountMeta(accounts.propertyHost),
      getAccountMeta(accounts.booking),
      getAccountMeta(accounts.config),
    ],
    data: getHostPendingAcceptInstructionDataEncoder().encode({}),
    programAddress,
  } as HostPendingAcceptInstruction<
    TProgramAddress,
    TAccountHost,
    TAccountPropertyHost,
    TAccountBooking,
    TAccountConfig
  >);
}

export type HostPendingAcceptInput<
  TAccountHost extends string = string,
  TAccountPropertyHost extends string = string,
  TAccountBooking extends string = string,
  TAccountConfig extends string = string,
> = {
  host: TransactionSigner<TAccountHost>;
  propertyHost: Address<TAccountPropertyHost>;
  booking: Address<TAccountBooking>;
  config: Address<TAccountConfig>;
};

export function getHostPendingAcceptInstruction<
  TAccountHost extends string,
  TAccountPropertyHost extends string,
  TAccountBooking extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof STAYKE_PROGRAM_ADDRESS,
>(
  input: HostPendingAcceptInput<
    TAccountHost,
    TAccountPropertyHost,
    TAccountBooking,
    TAccountConfig
  >,
  config?: { programAddress?: TProgramAddress },
): HostPendingAcceptInstruction<
  TProgramAddress,
  TAccountHost,
  TAccountPropertyHost,
  TAccountBooking,
  TAccountConfig
> {
  // Program address.
  const programAddress = config?.programAddress ?? STAYKE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    host: { value: input.host ?? null, isWritable: true },
    propertyHost: { value: input.propertyHost ?? null, isWritable: true },
    booking: { value: input.booking ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.host),
      getAccountMeta(accounts.propertyHost),
      getAccountMeta(accounts.booking),
      getAccountMeta(accounts.config),
    ],
    data: getHostPendingAcceptInstructionDataEncoder().encode({}),
    programAddress,
  } as HostPendingAcceptInstruction<
    TProgramAddress,
    TAccountHost,
    TAccountPropertyHost,
    TAccountBooking,
    TAccountConfig
  >);
}

export type ParsedHostPendingAcceptInstruction<
  TProgram extends string = typeof STAYKE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    host: TAccountMetas[0];
    propertyHost: TAccountMetas[1];
    booking: TAccountMetas[2];
    config: TAccountMetas[3];
  };
  data: HostPendingAcceptInstructionData;
};

export function parseHostPendingAcceptInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedHostPendingAcceptInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      host: getNextAccount(),
      propertyHost: getNextAccount(),
      booking: getNextAccount(),
      config: getNextAccount(),
    },
    data: getHostPendingAcceptInstructionDataDecoder().decode(instruction.data),
  };
}

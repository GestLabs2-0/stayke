/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { STAYKE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const WITHDRAW_GUARANTEE_DISCRIMINATOR = new Uint8Array([
  90, 175, 173, 229, 183, 148, 41, 242,
]);

export function getWithdrawGuaranteeDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    WITHDRAW_GUARANTEE_DISCRIMINATOR,
  );
}

export type WithdrawGuaranteeInstruction<
  TProgram extends string = typeof STAYKE_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountUserProfile extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountTreasuryTokenAccount extends string | AccountMeta<string> = string,
  TAccountTreasuryPda extends string | AccountMeta<string> = string,
  TAccountUserTokenAccount extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountUserProfile extends string
        ? WritableAccount<TAccountUserProfile>
        : TAccountUserProfile,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountTreasuryTokenAccount extends string
        ? WritableAccount<TAccountTreasuryTokenAccount>
        : TAccountTreasuryTokenAccount,
      TAccountTreasuryPda extends string
        ? ReadonlyAccount<TAccountTreasuryPda>
        : TAccountTreasuryPda,
      TAccountUserTokenAccount extends string
        ? WritableAccount<TAccountUserTokenAccount>
        : TAccountUserTokenAccount,
      TAccountMint extends string
        ? WritableAccount<TAccountMint>
        : TAccountMint,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type WithdrawGuaranteeInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type WithdrawGuaranteeInstructionDataArgs = { amount: number | bigint };

export function getWithdrawGuaranteeInstructionDataEncoder(): FixedSizeEncoder<WithdrawGuaranteeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: WITHDRAW_GUARANTEE_DISCRIMINATOR }),
  );
}

export function getWithdrawGuaranteeInstructionDataDecoder(): FixedSizeDecoder<WithdrawGuaranteeInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
  ]);
}

export function getWithdrawGuaranteeInstructionDataCodec(): FixedSizeCodec<
  WithdrawGuaranteeInstructionDataArgs,
  WithdrawGuaranteeInstructionData
> {
  return combineCodec(
    getWithdrawGuaranteeInstructionDataEncoder(),
    getWithdrawGuaranteeInstructionDataDecoder(),
  );
}

export type WithdrawGuaranteeAsyncInput<
  TAccountSigner extends string = string,
  TAccountUserProfile extends string = string,
  TAccountConfig extends string = string,
  TAccountTreasuryTokenAccount extends string = string,
  TAccountTreasuryPda extends string = string,
  TAccountUserTokenAccount extends string = string,
  TAccountMint extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  userProfile: Address<TAccountUserProfile>;
  config?: Address<TAccountConfig>;
  /** Treasury token account — source of the withdrawal. */
  treasuryTokenAccount: Address<TAccountTreasuryTokenAccount>;
  treasuryPda?: Address<TAccountTreasuryPda>;
  /** Destination: the user's own USDC token account. */
  userTokenAccount: Address<TAccountUserTokenAccount>;
  mint: Address<TAccountMint>;
  tokenProgram?: Address<TAccountTokenProgram>;
  amount: WithdrawGuaranteeInstructionDataArgs["amount"];
};

export async function getWithdrawGuaranteeInstructionAsync<
  TAccountSigner extends string,
  TAccountUserProfile extends string,
  TAccountConfig extends string,
  TAccountTreasuryTokenAccount extends string,
  TAccountTreasuryPda extends string,
  TAccountUserTokenAccount extends string,
  TAccountMint extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof STAYKE_PROGRAM_ADDRESS,
>(
  input: WithdrawGuaranteeAsyncInput<
    TAccountSigner,
    TAccountUserProfile,
    TAccountConfig,
    TAccountTreasuryTokenAccount,
    TAccountTreasuryPda,
    TAccountUserTokenAccount,
    TAccountMint,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  WithdrawGuaranteeInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountUserProfile,
    TAccountConfig,
    TAccountTreasuryTokenAccount,
    TAccountTreasuryPda,
    TAccountUserTokenAccount,
    TAccountMint,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? STAYKE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    userProfile: { value: input.userProfile ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    treasuryTokenAccount: {
      value: input.treasuryTokenAccount ?? null,
      isWritable: true,
    },
    treasuryPda: { value: input.treasuryPda ?? null, isWritable: false },
    userTokenAccount: {
      value: input.userTokenAccount ?? null,
      isWritable: true,
    },
    mint: { value: input.mint ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([99, 111, 110, 102, 105, 103])),
      ],
    });
  }
  if (!accounts.treasuryPda.value) {
    accounts.treasuryPda.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([116, 114, 101, 97, 115, 117, 114, 121]),
        ),
      ],
    });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.userProfile),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.treasuryTokenAccount),
      getAccountMeta(accounts.treasuryPda),
      getAccountMeta(accounts.userTokenAccount),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getWithdrawGuaranteeInstructionDataEncoder().encode(
      args as WithdrawGuaranteeInstructionDataArgs,
    ),
    programAddress,
  } as WithdrawGuaranteeInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountUserProfile,
    TAccountConfig,
    TAccountTreasuryTokenAccount,
    TAccountTreasuryPda,
    TAccountUserTokenAccount,
    TAccountMint,
    TAccountTokenProgram
  >);
}

export type WithdrawGuaranteeInput<
  TAccountSigner extends string = string,
  TAccountUserProfile extends string = string,
  TAccountConfig extends string = string,
  TAccountTreasuryTokenAccount extends string = string,
  TAccountTreasuryPda extends string = string,
  TAccountUserTokenAccount extends string = string,
  TAccountMint extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  userProfile: Address<TAccountUserProfile>;
  config: Address<TAccountConfig>;
  /** Treasury token account — source of the withdrawal. */
  treasuryTokenAccount: Address<TAccountTreasuryTokenAccount>;
  treasuryPda: Address<TAccountTreasuryPda>;
  /** Destination: the user's own USDC token account. */
  userTokenAccount: Address<TAccountUserTokenAccount>;
  mint: Address<TAccountMint>;
  tokenProgram?: Address<TAccountTokenProgram>;
  amount: WithdrawGuaranteeInstructionDataArgs["amount"];
};

export function getWithdrawGuaranteeInstruction<
  TAccountSigner extends string,
  TAccountUserProfile extends string,
  TAccountConfig extends string,
  TAccountTreasuryTokenAccount extends string,
  TAccountTreasuryPda extends string,
  TAccountUserTokenAccount extends string,
  TAccountMint extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof STAYKE_PROGRAM_ADDRESS,
>(
  input: WithdrawGuaranteeInput<
    TAccountSigner,
    TAccountUserProfile,
    TAccountConfig,
    TAccountTreasuryTokenAccount,
    TAccountTreasuryPda,
    TAccountUserTokenAccount,
    TAccountMint,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): WithdrawGuaranteeInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountUserProfile,
  TAccountConfig,
  TAccountTreasuryTokenAccount,
  TAccountTreasuryPda,
  TAccountUserTokenAccount,
  TAccountMint,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? STAYKE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    userProfile: { value: input.userProfile ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    treasuryTokenAccount: {
      value: input.treasuryTokenAccount ?? null,
      isWritable: true,
    },
    treasuryPda: { value: input.treasuryPda ?? null, isWritable: false },
    userTokenAccount: {
      value: input.userTokenAccount ?? null,
      isWritable: true,
    },
    mint: { value: input.mint ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.userProfile),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.treasuryTokenAccount),
      getAccountMeta(accounts.treasuryPda),
      getAccountMeta(accounts.userTokenAccount),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getWithdrawGuaranteeInstructionDataEncoder().encode(
      args as WithdrawGuaranteeInstructionDataArgs,
    ),
    programAddress,
  } as WithdrawGuaranteeInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountUserProfile,
    TAccountConfig,
    TAccountTreasuryTokenAccount,
    TAccountTreasuryPda,
    TAccountUserTokenAccount,
    TAccountMint,
    TAccountTokenProgram
  >);
}

export type ParsedWithdrawGuaranteeInstruction<
  TProgram extends string = typeof STAYKE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    userProfile: TAccountMetas[1];
    config: TAccountMetas[2];
    /** Treasury token account — source of the withdrawal. */
    treasuryTokenAccount: TAccountMetas[3];
    treasuryPda: TAccountMetas[4];
    /** Destination: the user's own USDC token account. */
    userTokenAccount: TAccountMetas[5];
    mint: TAccountMetas[6];
    tokenProgram: TAccountMetas[7];
  };
  data: WithdrawGuaranteeInstructionData;
};

export function parseWithdrawGuaranteeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedWithdrawGuaranteeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 8) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      userProfile: getNextAccount(),
      config: getNextAccount(),
      treasuryTokenAccount: getNextAccount(),
      treasuryPda: getNextAccount(),
      userTokenAccount: getNextAccount(),
      mint: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getWithdrawGuaranteeInstructionDataDecoder().decode(instruction.data),
  };
}

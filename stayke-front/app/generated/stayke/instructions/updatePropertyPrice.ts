/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { STAYKE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const UPDATE_PROPERTY_PRICE_DISCRIMINATOR = new Uint8Array([
  26, 120, 255, 136, 9, 83, 139, 105,
]);

export function getUpdatePropertyPriceDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_PROPERTY_PRICE_DISCRIMINATOR,
  );
}

export type UpdatePropertyPriceInstruction<
  TProgram extends string = typeof STAYKE_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountUserProfile extends string | AccountMeta<string> = string,
  TAccountProperty extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountUserProfile extends string
        ? WritableAccount<TAccountUserProfile>
        : TAccountUserProfile,
      TAccountProperty extends string
        ? WritableAccount<TAccountProperty>
        : TAccountProperty,
      ...TRemainingAccounts,
    ]
  >;

export type UpdatePropertyPriceInstructionData = {
  discriminator: ReadonlyUint8Array;
  newPricePerNight: bigint;
};

export type UpdatePropertyPriceInstructionDataArgs = {
  newPricePerNight: number | bigint;
};

export function getUpdatePropertyPriceInstructionDataEncoder(): FixedSizeEncoder<UpdatePropertyPriceInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newPricePerNight", getU64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: UPDATE_PROPERTY_PRICE_DISCRIMINATOR,
    }),
  );
}

export function getUpdatePropertyPriceInstructionDataDecoder(): FixedSizeDecoder<UpdatePropertyPriceInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newPricePerNight", getU64Decoder()],
  ]);
}

export function getUpdatePropertyPriceInstructionDataCodec(): FixedSizeCodec<
  UpdatePropertyPriceInstructionDataArgs,
  UpdatePropertyPriceInstructionData
> {
  return combineCodec(
    getUpdatePropertyPriceInstructionDataEncoder(),
    getUpdatePropertyPriceInstructionDataDecoder(),
  );
}

export type UpdatePropertyPriceInput<
  TAccountSigner extends string = string,
  TAccountUserProfile extends string = string,
  TAccountProperty extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  userProfile: Address<TAccountUserProfile>;
  property: Address<TAccountProperty>;
  newPricePerNight: UpdatePropertyPriceInstructionDataArgs["newPricePerNight"];
};

export function getUpdatePropertyPriceInstruction<
  TAccountSigner extends string,
  TAccountUserProfile extends string,
  TAccountProperty extends string,
  TProgramAddress extends Address = typeof STAYKE_PROGRAM_ADDRESS,
>(
  input: UpdatePropertyPriceInput<
    TAccountSigner,
    TAccountUserProfile,
    TAccountProperty
  >,
  config?: { programAddress?: TProgramAddress },
): UpdatePropertyPriceInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountUserProfile,
  TAccountProperty
> {
  // Program address.
  const programAddress = config?.programAddress ?? STAYKE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    userProfile: { value: input.userProfile ?? null, isWritable: true },
    property: { value: input.property ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.userProfile),
      getAccountMeta(accounts.property),
    ],
    data: getUpdatePropertyPriceInstructionDataEncoder().encode(
      args as UpdatePropertyPriceInstructionDataArgs,
    ),
    programAddress,
  } as UpdatePropertyPriceInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountUserProfile,
    TAccountProperty
  >);
}

export type ParsedUpdatePropertyPriceInstruction<
  TProgram extends string = typeof STAYKE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    userProfile: TAccountMetas[1];
    property: TAccountMetas[2];
  };
  data: UpdatePropertyPriceInstructionData;
};

export function parseUpdatePropertyPriceInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdatePropertyPriceInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      userProfile: getNextAccount(),
      property: getNextAccount(),
    },
    data: getUpdatePropertyPriceInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}

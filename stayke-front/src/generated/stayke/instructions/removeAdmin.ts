/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { STAYKE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const REMOVE_ADMIN_DISCRIMINATOR = new Uint8Array([
  74, 202, 71, 106, 252, 31, 72, 183,
]);

export function getRemoveAdminDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REMOVE_ADMIN_DISCRIMINATOR,
  );
}

export type RemoveAdminInstruction<
  TProgram extends string = typeof STAYKE_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      ...TRemainingAccounts,
    ]
  >;

export type RemoveAdminInstructionData = {
  discriminator: ReadonlyUint8Array;
  adminToRemove: Address;
};

export type RemoveAdminInstructionDataArgs = { adminToRemove: Address };

export function getRemoveAdminInstructionDataEncoder(): FixedSizeEncoder<RemoveAdminInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["adminToRemove", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: REMOVE_ADMIN_DISCRIMINATOR }),
  );
}

export function getRemoveAdminInstructionDataDecoder(): FixedSizeDecoder<RemoveAdminInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["adminToRemove", getAddressDecoder()],
  ]);
}

export function getRemoveAdminInstructionDataCodec(): FixedSizeCodec<
  RemoveAdminInstructionDataArgs,
  RemoveAdminInstructionData
> {
  return combineCodec(
    getRemoveAdminInstructionDataEncoder(),
    getRemoveAdminInstructionDataDecoder(),
  );
}

export type RemoveAdminAsyncInput<
  TAccountSigner extends string = string,
  TAccountConfig extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  config?: Address<TAccountConfig>;
  adminToRemove: RemoveAdminInstructionDataArgs["adminToRemove"];
};

export async function getRemoveAdminInstructionAsync<
  TAccountSigner extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof STAYKE_PROGRAM_ADDRESS,
>(
  input: RemoveAdminAsyncInput<TAccountSigner, TAccountConfig>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RemoveAdminInstruction<TProgramAddress, TAccountSigner, TAccountConfig>
> {
  // Program address.
  const programAddress = config?.programAddress ?? STAYKE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([99, 111, 110, 102, 105, 103])),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.config),
    ],
    data: getRemoveAdminInstructionDataEncoder().encode(
      args as RemoveAdminInstructionDataArgs,
    ),
    programAddress,
  } as RemoveAdminInstruction<TProgramAddress, TAccountSigner, TAccountConfig>);
}

export type RemoveAdminInput<
  TAccountSigner extends string = string,
  TAccountConfig extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  config: Address<TAccountConfig>;
  adminToRemove: RemoveAdminInstructionDataArgs["adminToRemove"];
};

export function getRemoveAdminInstruction<
  TAccountSigner extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof STAYKE_PROGRAM_ADDRESS,
>(
  input: RemoveAdminInput<TAccountSigner, TAccountConfig>,
  config?: { programAddress?: TProgramAddress },
): RemoveAdminInstruction<TProgramAddress, TAccountSigner, TAccountConfig> {
  // Program address.
  const programAddress = config?.programAddress ?? STAYKE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.config),
    ],
    data: getRemoveAdminInstructionDataEncoder().encode(
      args as RemoveAdminInstructionDataArgs,
    ),
    programAddress,
  } as RemoveAdminInstruction<TProgramAddress, TAccountSigner, TAccountConfig>);
}

export type ParsedRemoveAdminInstruction<
  TProgram extends string = typeof STAYKE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    config: TAccountMetas[1];
  };
  data: RemoveAdminInstructionData;
};

export function parseRemoveAdminInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRemoveAdminInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { signer: getNextAccount(), config: getNextAccount() },
    data: getRemoveAdminInstructionDataDecoder().decode(instruction.data),
  };
}

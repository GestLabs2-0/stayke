/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { STAYKE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const CLOSE_BOOKING_DISCRIMINATOR = new Uint8Array([
  5, 218, 54, 100, 74, 141, 66, 111,
]);

export function getCloseBookingDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_BOOKING_DISCRIMINATOR,
  );
}

export type CloseBookingInstruction<
  TProgram extends string = typeof STAYKE_PROGRAM_ADDRESS,
  TAccountClient extends string | AccountMeta<string> = string,
  TAccountClientProfile extends string | AccountMeta<string> = string,
  TAccountHostProfile extends string | AccountMeta<string> = string,
  TAccountProperty extends string | AccountMeta<string> = string,
  TAccountBooking extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountClient extends string
        ? WritableSignerAccount<TAccountClient> &
            AccountSignerMeta<TAccountClient>
        : TAccountClient,
      TAccountClientProfile extends string
        ? WritableAccount<TAccountClientProfile>
        : TAccountClientProfile,
      TAccountHostProfile extends string
        ? WritableAccount<TAccountHostProfile>
        : TAccountHostProfile,
      TAccountProperty extends string
        ? WritableAccount<TAccountProperty>
        : TAccountProperty,
      TAccountBooking extends string
        ? WritableAccount<TAccountBooking>
        : TAccountBooking,
      ...TRemainingAccounts,
    ]
  >;

export type CloseBookingInstructionData = {
  discriminator: ReadonlyUint8Array;
  score: number;
};

export type CloseBookingInstructionDataArgs = { score: number };

export function getCloseBookingInstructionDataEncoder(): FixedSizeEncoder<CloseBookingInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["score", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CLOSE_BOOKING_DISCRIMINATOR }),
  );
}

export function getCloseBookingInstructionDataDecoder(): FixedSizeDecoder<CloseBookingInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["score", getU8Decoder()],
  ]);
}

export function getCloseBookingInstructionDataCodec(): FixedSizeCodec<
  CloseBookingInstructionDataArgs,
  CloseBookingInstructionData
> {
  return combineCodec(
    getCloseBookingInstructionDataEncoder(),
    getCloseBookingInstructionDataDecoder(),
  );
}

export type CloseBookingInput<
  TAccountClient extends string = string,
  TAccountClientProfile extends string = string,
  TAccountHostProfile extends string = string,
  TAccountProperty extends string = string,
  TAccountBooking extends string = string,
> = {
  client: TransactionSigner<TAccountClient>;
  clientProfile: Address<TAccountClientProfile>;
  hostProfile: Address<TAccountHostProfile>;
  property: Address<TAccountProperty>;
  booking: Address<TAccountBooking>;
  score: CloseBookingInstructionDataArgs["score"];
};

export function getCloseBookingInstruction<
  TAccountClient extends string,
  TAccountClientProfile extends string,
  TAccountHostProfile extends string,
  TAccountProperty extends string,
  TAccountBooking extends string,
  TProgramAddress extends Address = typeof STAYKE_PROGRAM_ADDRESS,
>(
  input: CloseBookingInput<
    TAccountClient,
    TAccountClientProfile,
    TAccountHostProfile,
    TAccountProperty,
    TAccountBooking
  >,
  config?: { programAddress?: TProgramAddress },
): CloseBookingInstruction<
  TProgramAddress,
  TAccountClient,
  TAccountClientProfile,
  TAccountHostProfile,
  TAccountProperty,
  TAccountBooking
> {
  // Program address.
  const programAddress = config?.programAddress ?? STAYKE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    client: { value: input.client ?? null, isWritable: true },
    clientProfile: { value: input.clientProfile ?? null, isWritable: true },
    hostProfile: { value: input.hostProfile ?? null, isWritable: true },
    property: { value: input.property ?? null, isWritable: true },
    booking: { value: input.booking ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.client),
      getAccountMeta(accounts.clientProfile),
      getAccountMeta(accounts.hostProfile),
      getAccountMeta(accounts.property),
      getAccountMeta(accounts.booking),
    ],
    data: getCloseBookingInstructionDataEncoder().encode(
      args as CloseBookingInstructionDataArgs,
    ),
    programAddress,
  } as CloseBookingInstruction<
    TProgramAddress,
    TAccountClient,
    TAccountClientProfile,
    TAccountHostProfile,
    TAccountProperty,
    TAccountBooking
  >);
}

export type ParsedCloseBookingInstruction<
  TProgram extends string = typeof STAYKE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    client: TAccountMetas[0];
    clientProfile: TAccountMetas[1];
    hostProfile: TAccountMetas[2];
    property: TAccountMetas[3];
    booking: TAccountMetas[4];
  };
  data: CloseBookingInstructionData;
};

export function parseCloseBookingInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCloseBookingInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      client: getNextAccount(),
      clientProfile: getNextAccount(),
      hostProfile: getNextAccount(),
      property: getNextAccount(),
      booking: getNextAccount(),
    },
    data: getCloseBookingInstructionDataDecoder().decode(instruction.data),
  };
}
